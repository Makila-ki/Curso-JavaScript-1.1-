let resultado = 8/(2*(2+2));
// el orden de operaciones es importante, primero se resuelve lo que está dentro del paréntesis, 
// luego la multiplicación y finalmente la división
console.log(resultado);